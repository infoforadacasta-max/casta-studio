const menu=document.querySelector('.menu');menu?.addEventListener('click',()=>{const expanded=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!expanded));document.getElementById('links')?.classList.toggle('open',!expanded)});
document.querySelectorAll('.submenu-toggle').forEach(button=>button.addEventListener('click',()=>{const expanded=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!expanded));button.closest('.nav-group')?.classList.toggle('submenu-open',!expanded)}));
const christmasPacks=document.getElementById('gift-christmas-packs');
document.querySelectorAll('input[name="session"]').forEach(option=>option.addEventListener('change',()=>{const isChristmas=option.checked&&option.value==='Mini-sessão de Natal';christmasPacks.hidden=!isChristmas;christmasPacks.querySelectorAll('input').forEach(input=>{input.required=isChristmas;if(!isChristmas)input.checked=false})}));
document.getElementById('gift-form').addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;if(!form.reportValidity())return;const data=new FormData(form);const message=`Olá Nádia e Ruben! Gostava de oferecer um voucher CASTA.\n\nSessão: ${data.get('session')}${data.get('christmasPack')?`\nPack: ${data.get('christmasPack')}`:''}\nPara: ${data.get('recipient')}\nDe: ${data.get('sender')}\nEmail: ${data.get('email')}${data.get('phone')?`\nTelefone: ${data.get('phone')}`:''}${data.get('dedication')?`\nDedicatória: ${data.get('dedication')}`:''}\n\nPodem enviar-me as opções, o valor e as instruções para o voucher digital?`;window.open(`https://wa.me/351919592819?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer')});
